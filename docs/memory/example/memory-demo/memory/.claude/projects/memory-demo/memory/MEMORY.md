# MEMORY

## Stored topics

- debugging.md: recurring failure modes and how to check them
- preferences.md: local workflow preferences and commands

## Stable facts

- Use `pnpm build` for production builds.
- Use `pnpm test` for validation.
- API contract drift is the most common regression risk.
