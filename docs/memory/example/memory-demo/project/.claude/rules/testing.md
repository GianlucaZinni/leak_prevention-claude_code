---
paths:
  - "src/**/*.{ts,tsx}"
  - "tests/**/*.test.ts"
---

# Testing rules

- Add or update a regression test when behavior changes.
- Prefer the smallest test that proves the behavior.
- Run `pnpm test` before finalizing a change.
